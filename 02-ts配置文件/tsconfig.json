{
  // 使用配置文件之后，不能使用tec 文件名，这样会忽略配置文件
  "compilerOptions": {
    "target": "es2016",   // 转换成的语言
    "module": "commonjs", // 配置编译目标使用的模块化标准
    "lib": ["es2016"],  // ts执行的环境，默认是dom浏览器环境
    "outDir": "./dist",  // 将编译结果放到那个目录下
  },
  "include": ["./src"],  // 编译文件的路径下的所有文件
}
